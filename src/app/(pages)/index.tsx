import { ScrollView } from "react-native";
import ProcessHandoffSection from "src/ProcessHandoffSection";



export default function Index() {
  return (
    <ScrollView>
      <ProcessHandoffSection />
    </ScrollView>
  );
}